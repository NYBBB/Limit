随时记录当前修改和进度，将还没实现的、已经实现的，都随时记录

同一个命令最多运行两次，比如dotnet build，运行两次后，如果还是失败，那么就不再运行了，并且告诉用户，并让用户去修改。

所有的回答和代码中的注释，还有文档文件，都必须是中文的。

你不需要主动去运行dotnet build，dotnet publish，或者dotnet run，这些命令。因为你运行后看不到报错，我会自己去运行。