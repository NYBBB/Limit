<?xml version="1.0" encoding="utf-8"?>
<!-- 
    HeatmapControl.xaml - Phase 3: 精力热力图
    7天 × 24小时 Grid 布局（自适应宽度）
-->
<UserControl
    x:Class="EyeGuard.UI.Controls.HeatmapControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EyeGuard.UI.Controls">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 小时标签行 -->
        <Grid Grid.Row="0" Margin="50,0,0,4" x:Name="HourLabelsGrid"/>
        
        <!-- 热力图主体 -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="50"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- 日期标签 (7天) -->
            <StackPanel Grid.Column="0" x:Name="DayLabelsPanel"/>
            
            <!-- 热力图格子容器 -->
            <StackPanel Grid.Column="1" x:Name="HeatmapGrid"/>
        </Grid>
    </Grid>
</UserControl>
