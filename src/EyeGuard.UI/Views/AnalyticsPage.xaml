<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="EyeGuard.UI.Views.AnalyticsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="Transparent">

    <ScrollViewer Padding="32">
        <StackPanel Spacing="24">
            
            <!-- 时间范围选择器 -->
            <StackPanel Orientation="Horizontal" 
                        HorizontalAlignment="Center" 
                        Spacing="16">
                
                <!-- 日期范围 -->
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <RadioButton Content="过去7天" IsChecked="True" GroupName="DateRange"/>
                    <RadioButton Content="过去28天" GroupName="DateRange"/>
                    <RadioButton Content="过去90天" GroupName="DateRange"/>
                </StackPanel>
                
                <Border Width="1" Background="{ThemeResource DividerStrokeColorDefaultBrush}"/>
                
                <!-- 聚合方式 -->
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <RadioButton Content="按天" IsChecked="True" GroupName="Aggregation"/>
                    <RadioButton Content="按周" GroupName="Aggregation"/>
                </StackPanel>
            </StackPanel>

            <!-- 使用时长图表 -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="8" Padding="24">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <TextBlock Text="使用时长" FontSize="20" FontWeight="SemiBold"/>
                        <FontIcon Glyph="&#xE946;" FontSize="14" 
                                  Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    </StackPanel>
                    
                    <!-- 柱状图占位区 -->
                    <Border Height="240" Margin="0,16,0,0"
                            CornerRadius="8"
                            Background="{ThemeResource LayerFillColorDefaultBrush}">
                        <TextBlock Text="使用时长柱状图将在此处显示" 
                                   HorizontalAlignment="Center" 
                                   VerticalAlignment="Center"
                                   FontSize="12"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    </Border>
                </StackPanel>
            </Border>

            <!-- 疲劳峰值时长图表 -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="8" Padding="24">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <TextBlock Text="疲劳峰值时长" FontSize="20" FontWeight="SemiBold"/>
                        <FontIcon Glyph="&#xE946;" FontSize="14" 
                                  Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    </StackPanel>
                    
                    <Border Height="160" Margin="0,16,0,0"
                            CornerRadius="8"
                            Background="{ThemeResource LayerFillColorDefaultBrush}">
                        <TextBlock Text="图表数据加载中..." 
                                   HorizontalAlignment="Center" 
                                   VerticalAlignment="Center"
                                   FontSize="12"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    </Border>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</Page>
