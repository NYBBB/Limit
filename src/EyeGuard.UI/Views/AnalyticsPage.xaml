<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="EyeGuard.UI.Views.AnalyticsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:lvc="using:LiveChartsCore.SkiaSharpView.WinUI"
    xmlns:vm="using:EyeGuard.UI.ViewModels"
    Background="Transparent">
    
    <Page.DataContext>
        <vm:AnalyticsViewModel/>
    </Page.DataContext>

    <ScrollViewer Padding="32">
        <StackPanel Spacing="24">
            
            <TextBlock Text="数据分析" FontSize="28" FontWeight="SemiBold"/>
            
            <!-- 24小时应用使用分布 -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="8" Padding="24">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="12" Margin="0,0,0,16">
                        <TextBlock Text="24小时应用使用分布" FontSize="20" FontWeight="SemiBold"/>
                        <TextBlock Text="(今日)" 
                                   FontSize="14" 
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   VerticalAlignment="Bottom"
                                   Margin="0,0,0,2"/>
                    </StackPanel>
                    
                    <!-- LiveCharts2 柱状图 -->
                    <lvc:CartesianChart 
                        Height="300"
                        Series="{Binding HourlyUsageSeries}"
                        XAxes="{Binding XAxes}"
                        YAxes="{Binding YAxes}"
                        LegendPosition="Bottom"
                        TooltipPosition="Top"/>
                    
                    <TextBlock Text="💡 提示：图表展示每小时内全天使用最多的前8个应用 + 其他应用" 
                               FontSize="12" 
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               Margin="0,12,0,0"/>
                </StackPanel>
            </Border>

            <!-- 疲劳趋势图 (占位) -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="8" Padding="24">
                <StackPanel>
                    <TextBlock Text="疲劳趋势" FontSize="20" FontWeight="SemiBold"/>
                    
                    <Border Height="200" Margin="0,16,0,0"
                            CornerRadius="8"
                            Background="{ThemeResource LayerFillColorDefaultBrush}">
                        <TextBlock Text="历史疲劳趋势功能即将推出 (P1.2)" 
                                   HorizontalAlignment="Center" 
                                   VerticalAlignment="Center"
                                   FontSize="12"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    </Border>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</Page>
