<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="EyeGuard.UI.Views.AnalyticsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:lvc="using:LiveChartsCore.SkiaSharpView.WinUI"
    xmlns:vm="using:EyeGuard.UI.ViewModels"
    Background="Transparent">
    
    <Page.DataContext>
        <vm:AnalyticsViewModel/>
    </Page.DataContext>

    <ScrollViewer Padding="32">
        <StackPanel Spacing="24">
            
            <TextBlock Text="数据分析" FontSize="28" FontWeight="SemiBold"/>
            
            <!-- 日期选择器 -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="8" Padding="16">
                <StackPanel Orientation="Horizontal" Spacing="12">
                    <TextBlock Text="📅 日期选择:" VerticalAlignment="Center" FontWeight="SemiBold"/>
                    
                    <Button Content="今天" Command="{Binding SelectTodayCommand}" MinWidth="80"/>
                    <Button Content="昨天" Command="{Binding SelectYesterdayCommand}" MinWidth="80"/>
                    <Button Content="一周前" Command="{Binding SelectWeekAgoCommand}" MinWidth="80"/>
                    
                    <CalendarDatePicker 
                        Date="{Binding SelectedDate, Mode=TwoWay}"
                        PlaceholderText="选择日期"
                        MinWidth="150"/>
                    
                    <TextBlock Text="{Binding SelectedDateText}" 
                               VerticalAlignment="Center" 
                               FontSize="14"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                </StackPanel>
            </Border>
            
            <!-- 疲劳趋势图 -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="8" Padding="24">
                <StackPanel>
                    <TextBlock Text="疲劳趋势" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,16"/>
                    
                    <lvc:CartesianChart 
                        Height="250"
                        Series="{Binding FatigueTrendSeries}"
                        XAxes="{Binding FatigueXAxes}"
                        YAxes="{Binding FatigueYAxes}"
                        TooltipPosition="Top"
                        TooltipTextPaint="{Binding LegendPaint}"/>
                </StackPanel>
            </Border>
            
            <!-- 24小时应用使用分布 -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="8" Padding="24">
                <StackPanel>
                    <TextBlock Text="24小时应用使用分布" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,16"/>
                    
                    <!-- LiveCharts2 柱状图 -->
                    <lvc:CartesianChart 
                        Height="300"
                        Series="{Binding HourlyUsageSeries}"
                        XAxes="{Binding UsageXAxes}"
                        YAxes="{Binding UsageYAxes}"
                        LegendPosition="Bottom"
                        LegendTextPaint="{Binding LegendPaint}"
                        TooltipPosition="Top"
                        TooltipTextPaint="{Binding LegendPaint}"/>
                    
                    <TextBlock Text="💡 提示：图表展示每小时内全天使用最多的前8个应用 + 其他应用" 
                               FontSize="12" 
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               Margin="0,12,0,0"/>
                </StackPanel>
            </Border>
            
            <!-- Phase 6: 精力分布 + Grind统计 并排 -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <!-- 精力分布饼图 -->
                <Border Grid.Column="0"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1" CornerRadius="8" Padding="24"
                        Margin="0,0,12,0">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <TextBlock Text="🥧" FontSize="20"/>
                            <TextBlock Text="精力分布" FontSize="20" FontWeight="SemiBold"/>
                        </StackPanel>
                        <TextBlock Text="按工作/娱乐/沟通分类的时间分布" 
                                   FontSize="12" 
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   Margin="0,4,0,16"/>
                        
                        <lvc:PieChart 
                            Height="250"
                            Series="{Binding EnergyPieSeries}"
                            LegendPosition="Right"
                            LegendTextPaint="{Binding LegendPaint}"
                            TooltipTextPaint="{Binding LegendPaint}"/>
                        
                        <TextBlock Margin="0,12,0,0">
                            <Run Text="总活跃时间: " Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            <Run Text="{Binding TotalActiveMinutes}" FontWeight="Bold"/>
                            <Run Text=" 分钟" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        </TextBlock>
                    </StackPanel>
                </Border>
                
                <!-- The Grind 统计 -->
                <Border Grid.Column="1"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1" CornerRadius="8" Padding="24"
                        Margin="12,0,0,0">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <TextBlock Text="🔥" FontSize="20"/>
                            <TextBlock Text="The Grind" FontSize="20" FontWeight="SemiBold"/>
                        </StackPanel>
                        <TextBlock Text="高强度工作统计" 
                                   FontSize="12" 
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   Margin="0,4,0,24"/>
                        
                        <!-- 最长连续工作 -->
                        <TextBlock Text="今日最长连续工作" 
                                   FontSize="12" 
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        <StackPanel Orientation="Horizontal" Margin="0,4,0,20">
                            <TextBlock Text="{Binding LongestWorkSession}" 
                                       FontSize="36" FontWeight="Bold"/>
                            <TextBlock Text=" 分钟" 
                                       FontSize="14" VerticalAlignment="Bottom" 
                                       Margin="4,0,0,8"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        </StackPanel>
                        
                        <!-- 过载时间 -->
                        <TextBlock Text="过载时间 (疲劳 ≥ 80%)" 
                                   FontSize="12" 
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        <StackPanel Orientation="Horizontal" Margin="0,4,0,8">
                            <TextBlock Text="{Binding OverloadMinutes}" 
                                       FontSize="24" FontWeight="SemiBold"
                                       Foreground="#F44336"/>
                            <TextBlock Text=" 分钟" 
                                       FontSize="12" VerticalAlignment="Bottom" 
                                       Margin="4,0,0,4"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            <TextBlock Text=" (" 
                                       FontSize="12" VerticalAlignment="Bottom" 
                                       Margin="8,0,0,4"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            <TextBlock Text="{Binding OverloadPercentage}" 
                                       FontSize="12" VerticalAlignment="Bottom" 
                                       Margin="0,0,0,4"
                                       Foreground="#F44336"/>
                            <TextBlock Text="%)" 
                                       FontSize="12" VerticalAlignment="Bottom" 
                                       Margin="0,0,0,4"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        </StackPanel>
                        
                        <ProgressBar Value="{Binding OverloadPercentage}" Maximum="100"
                                     Foreground="#F44336"/>
                    </StackPanel>
                </Border>
            </Grid>
            
            <!-- Phase 6 P1: Daily Rhythm 日节奏图 -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="8" Padding="24">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <TextBlock Text="📊" FontSize="20"/>
                        <TextBlock Text="Daily Rhythm 日节奏" FontSize="20" FontWeight="SemiBold"/>
                    </StackPanel>
                    <TextBlock Text="24小时疲劳曲线" 
                               FontSize="12" 
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               Margin="0,4,0,16"/>
                    
                    <lvc:CartesianChart 
                        Height="250"
                        Series="{Binding DailyRhythmSeries}"
                        XAxes="{Binding DailyRhythmXAxes}"
                        YAxes="{Binding DailyRhythmYAxes}"
                        TooltipPosition="Top"
                        TooltipTextPaint="{Binding LegendPaint}"/>
                </StackPanel>
            </Border>
            
            <!-- Phase 6 P2: Weekly Trends 周趋势 -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="8" Padding="24">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <TextBlock Text="📈" FontSize="20"/>
                        <TextBlock Text="Weekly Trends 周趋势" FontSize="20" FontWeight="SemiBold"/>
                    </StackPanel>
                    <TextBlock Text="过去7天疲劳峰值与平均值对比" 
                               FontSize="12" 
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               Margin="0,4,0,16"/>
                    
                    <lvc:CartesianChart 
                        Height="250"
                        Series="{Binding WeeklyTrendsSeries}"
                        XAxes="{Binding WeeklyTrendsXAxes}"
                        YAxes="{Binding WeeklyTrendsYAxes}"
                        LegendPosition="Bottom"
                        LegendTextPaint="{Binding LegendPaint}"
                        TooltipPosition="Top"
                        TooltipTextPaint="{Binding LegendPaint}"/>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</Page>
